#include <WString.h>
namespace accesspoint
{
    void handleRoot();
    void handleSave();
    void handleRestart();
    void setupAccessPoint();
    bool isAccessPointActive();
    void loopAccessPoint();
}